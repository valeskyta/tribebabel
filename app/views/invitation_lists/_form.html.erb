<%= form_for(@invitation_list) do |f| %>
  <% if @invitation_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invitation_list.errors.count, "error") %> prohibited this invitation_list from being saved:</h2>

      <ul>
      <% @invitation_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.number_field :status %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
