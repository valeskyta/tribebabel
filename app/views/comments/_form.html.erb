	<div class="panel panel-default">

		<div class="panel-heading">
			<%= form_for [@post, @comment] do |f| %>
			<%= f.label :comment %>
			<%= f.text_area :comment %>

			<%= f.submit "enviar" %>
			<% end %>
		</div>
	</div>

